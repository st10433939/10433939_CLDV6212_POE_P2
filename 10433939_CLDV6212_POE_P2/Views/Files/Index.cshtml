@model List<_10433939_CLDV6212_POE_P2.Models.FileModel>

<h1>Files</h1>

<!--Display Messages-->
@if (TempData["Message"] != null)
{
    <div class="alert aletr-info">
        @TempData["Message"]
    </div>
}

<!--Upload Form-->
<form asp-action="UploadFile" method="post" enctype="multipart/form-data">
    <div class="form-group">
        <label for="file">Choose a file to upload:</label>
        <input type="file" name="file" class="form-control" />
        <button type="submit" class="btn btn-primary mt-2">Upload</button>
    </div>
</form>
<!--Display Uploaded file-->
@if (Model != null && Model.Count > 0)
{
    <ul>
        @foreach (var file in Model)
        {
            <li>
                @file.Name -
                Size: @file.DisplaySize -
                Last Modified: @file.LastModified?.ToString("g")
                - <a asp-action="DownloadFile" asp-route-fileName="@file.Name">Download</a>
            </li>
        }
    </ul>
}
else
{
    <p>No files found or model is null.</p>
}